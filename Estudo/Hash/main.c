#include "stdio.h"
#include "stdlib.h"
#include "TabelaHash.h"

int main(){
    // Hash *ha;
    // ha = cria_hash(1427);

    // int x, mat;

    // x = insere_hash_sem_colisao(ha, aluno1);

    // mat = aluno1.matricula;
    // x = busca_hash_sem_colisao(ha, mat, &aluno2);

    // x = insere_hash_enderco_aberto(ha, aluno1);

    // x = busca_hash_endereco_aberto(ha, mat, &aluno2);

    // libera_hash(ha);
}